---
import "@styles/global.css";
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";
import StickyCTA from "@components/StickyCTA.astro";
import barber from "@data/barber.json";
import SiteBackground from "@components/SiteBackground.astro";

type Props = {
  title: string;
  description: string;
  ogImage?: string; // opcional: override por página
};

const { title, description, ogImage } = Astro.props;
const url = new URL(Astro.request.url);
const canonical = url.toString();

// OG por defecto desde data
const og = ogImage ?? barber?.media?.og ?? "/images/og-cover.png";
---
<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />

    <title>{title}</title>

    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={og} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="theme-color" content="#07090B" />
  </head>

  <body class="text-zinc-100">
     <SiteBackground />

    <Header currentPath={url.pathname} brand={barber.nombre} whatsapp={barber.whatsapp} />

    <main class="mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8 pt-24 pb-24">
      <slot />
    </main>

    <Footer brand={barber.nombre} city={barber.ciudad} address={barber.direccion} />
    <StickyCTA brand={barber.nombre} whatsapp={barber.whatsapp} />
  </body>
</html>